{
  "name": "ruangperpus-api",
  "version": "1.0.0",
  "description": "RuangPerpus API server (json-server)",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
